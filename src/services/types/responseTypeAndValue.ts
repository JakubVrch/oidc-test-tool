export enum ResponseModeValue {
  QUERY = "query",
  FRAGMENT = "fragment",
  FORM_POST = "form_post",
}

export enum ResponseTypeValue {
  CODE = "code",
  ID_TOKEN = "id_token",
  TOKEN = "token",
}
